streamlit>=1.30.0
pandas>=2.0.0
plotly>=5.18.0
numpy>=1.24.0
duckdb>=1.0.0

# Export support
openpyxl>=3.1.0

# UI component libraries
streamlit-extras>=0.4.0
streamlit-shadcn-ui>=0.1.0

# Diagram rendering (metadata pages)
graphviz>=0.20.0

# Semantic layer + Knowledge graph
requests>=2.28.0     # Cube REST API client
neo4j>=5.0.0         # Neo4j Python driver for knowledge graph queries

# AI Assistant tab
openai>=1.30.0       # OpenAI-compatible SDK used with OpenRouter
python-dotenv>=1.0.0 # Loads .env file into environment variables

# Dev / testing
pytest>=7.0.0
pytest-cov>=4.0.0    # Coverage measurement (used in CI and `make coverage`)
ruff>=0.8.0          # Linter + import sorting
